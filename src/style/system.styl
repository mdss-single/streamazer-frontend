/*  <vars>  */
fontFira = 'Fira Sans Condensed';
fontOpen = 'Open Sans';
/*  </vars>  */

/*  <animations>  */
@keyframes stars
	from
		transform:translateX(0) rotateZ(360deg)
	to
		transform:translateX(-50%) rotateZ(360deg)
@keyframes fade
	from
		opacity 0
	to
		opacity 1
/*  </animations>  */